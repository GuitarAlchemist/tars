Here is the generated `package.json` file:

```json
{
  "name": "calculator-app",
  "version": "0.1.0",
  "dependencies": {
    "@babel/preset-env": "^7.16.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "webpack": "^5.51.0"
  },
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/preset-react": "^7.16.0",
    "babel-loader": "^8.2.3",
    "css-loader": "^6.5.1",
    "file-loader": "^6.2.0",
    "mini-css-extract-plugin": "^2.4.0",
    "react-scripts": "5.0.1",
    "style-loader": "^3.3.1",
    "webpack-cli": "^4.8.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test"
  }
}
```

This `package.json` file includes the necessary dependencies and devDependencies for a React.js project, including Webpack and Babel. The scripts section defines common commands such as starting the development server, building the application, and running tests.

Note that this is just one possible way to configure the package.json file for a React.js project. You may need to adjust it based on your specific requirements and dependencies.